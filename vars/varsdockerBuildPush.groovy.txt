def call(String imageName, String dockerHubUser) {
    echo "Building and Pushing Docker Image: ${imageName}"
    sh "docker build -t ${imageName} -f path/to/your/Dockerfile ."
    sh "docker push ${dockerHubUser}/${imageName}:latest"
}